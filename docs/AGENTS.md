# docs/ 운영 가드

이 폴더는 카공지도 박제 산출물 전용 공간이다.
PRD, ERD, FE-SPEC, BE-SPEC, BACKLOG만 이 경로에서 관리한다.
채점기, 앱 코드, 보호 시드, 골든값 원본은 여기로 옮기지 않는다.
골든 숫자 하드코딩 금지: 산출물 본문은 측정방법과 출처를 적고, 실측은 사람 소유 시드에서 재도출한다.
`app/tests/protected/cafes.seed.json`, `app/src/*`, `harness/`, `verify.sh`는 수정 금지 대상이다.
출처 구분: 사전기획은 `/Users/sjwoo/dev/kagong-plan-v1.md`, 현장박제는 `docs/product/` 산출물이다.
